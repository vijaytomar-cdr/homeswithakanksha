/**
 * External integration seams:
 * - Connect the selected IDX/MLS adapter after vendor credentials arrive.
 * - CRM submissions now flow through lib/crm and app/api/leads.
 * - Load approved Google Reviews through a server-side provider.
 * - Add GA4/GTM, Meta Pixel and Google Ads only after consent is configured.
 * - Add social profiles only after verified URLs are supplied.
 */
